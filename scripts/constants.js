// Invincibility. Testing only. Please. (default: false)
export const invincible = true;

// Radius of player's hitbox (default: 8)
export const hitboxRadius = 8;

// Radius of player's graze hitbox (default: 60)
export const grazeRadius = 60;

// Speed of player (default: 250)
export const playerSpeed = 250;

// Distance from edge of viewable area until droplets automatically disappear (default: 50)
export const outerMargin = 50;

// How transparent warnings should be right before they disappear (default: 0.75)
export const warningAlpha = 0.5;

export const songRefreshRate = 2;

export const musicVolume = 0.1;

export const soundEffectVolume = 0.1;

// How smooth the flickering effect is, higher is smoother but more delayed (default: 5)
export const flickerSmoothLen = 4;

// add detuning constant? or even make an in-game setting?????